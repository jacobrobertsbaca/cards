# Optional locally. The GitHub Pages build injects these from repo secrets.
# Without them, games only sync across tabs in this browser.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
