import type { BridgeSettings } from "./types"

export function bridgeRulesLine(_settings: BridgeSettings) {
  return "Rubber bridge · 4 players"
}

export function bridgeGameTooltip(_settings: BridgeSettings) {
  return "Rubber bridge — bid and play to win two games"
}

export function bridgeHistoryTooltip(_settings: BridgeSettings) {
  return "Rubber bridge"
}
